version: 2.1
jobs:
  build-and-patch:
    docker:
    # specify the version
      - image: circleci/node:latest

    steps:
      - run: git clone https://github.com/snyk/snyk
      - run: cd snyk && npm install --save-dev pkg
      - run: cd snyk && npm install
      - run: cd snyk && npx pkg . && ls -l
      - run: cd snyk && git describe --tags > VERSION
      - persist_to_workspace:
          root: .
          paths:
            - snyk/snyk-*
            - snyk/VERSION

  publish-github-release:
    docker:
      - image: gcr.io/snyk-technical-services/cicd-github
        auth:
          username: _json_key
          password: $GCLOUD_GCR_SNYK_TS_READER
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run: ls && ls snyk/  
      - run:
          name: "Publish Release on GitHub"
          command: | 
            VERSION=$(cat snyk/VERSION)
            ghr -t ${GITHUB_TOKEN} -u ${CIRCLE_PROJECT_USERNAME} -r ${CIRCLE_PROJECT_REPONAME} -c ${CIRCLE_SHA1} ${VERSION} snyk/

workflows:
  version: 2.1
  nightly:
    triggers:
      - schedule:
          cron: "0 0 * * *"
          filters:
            branches:
              only:
                - master
    jobs:
      - build-and-patch
  build-and-patch:
    jobs:
      - build-and-patch:
          filters:
            branches:
              only:
                  - master
      - publish-github-release:
          context: SNYK
          requires:
            - build-and-patch
          filters:
            branches:
              only:
                - master